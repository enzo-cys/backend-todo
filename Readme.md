# Backend ToDo

## Commande NPM

- npm init -y
- npm install express dotenv cors mysql2
- npm install --save-dev nodemon

**Pour supprimer un package NPM**

- npm remoeve nom_du_package

## Commande du terminal pendant la démo

- Voir toutes les commandes passées du terminal :
    - history

    ex.
    ```bash
    1  cd C:\laragon\www\backend-todo
    2  npm init -y
    3  npm install express dotenv cors mysql2
    4  npm install --save-dev nodemon
    ```